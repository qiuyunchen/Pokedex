import React from 'react';

const Stats = (props) => {
    const name = props.name;
    const url = props.url;
    
    return (
        <div>
            POKEMON STATS
        </div>
    )
}

export default Stats;